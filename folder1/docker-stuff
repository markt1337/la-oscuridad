# Docker Stuff



### Select all containers by ID
docker ps -a --format "{{.ID}}"   or docker ps -a -q

#### Quick delete all containers
docker ps -a --format "docker rm {{.ID}}" > delall.txt
cat delall.txt
